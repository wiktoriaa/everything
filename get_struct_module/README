This module create misc char device which write function returns pointer to struct module and write some informations about module in kernel logs.
To use device you haven`t to be root but to load module you must. 
Using example:
echo name_module > /dev/new_misc_device

MODULE ISN`T FINISHED and probably it will never been finished. 
